# 🔒 DevOps 脚本安全规范

> 适用于本仓库所有脚本（Shell / Python / Ansible / 任何涉及系统变更的代码）。
> 每个新脚本在提交前必须逐条自查，PR review 以此为 checklist。

---

## 0. 最小权限原则

脚本只拿它需要的权限，不多不少。

```
□ 明确声明所需权限（root / sudo / 普通用户），脚本入口第一件事就是检查
□ 不需要 root 的操作用普通用户跑，需要 root 的用 sudo
□ 临时文件放在安全的目录（/tmp 用 mktemp，不要用可预测的文件名）
□ 敏感变量（密码 / token / key）不进 git，不写死，不 echo 到终端
□ 能只读绝不写，能 append 绝不全量覆写
```

**反例：** 不检查 `$EUID` 直接用 `systemctl restart`，普通用户跑就报错但说不清楚原因。

---

## 1. 输入不可信

所有来自脚本外部的数据都是敌意的，直到验证通过。

```
□ CLI 参数：做范围/格式/白名单校验
□ 文件内容：检查文件存在、非空、格式合法后再消费
□ stdin / 交互输入：不信任，不直接拼接到命令
□ 环境变量：用前检查是否存在、格式是否合法
□ 网络数据 / API 返回：检查非空、结构合法后再用
```

**核心规则：先验证，后消费。永远不在验过的数据上再验一次。**

**反例：**
```bash
# ❌ 用户输入直接拼接，空格/特殊字符导致命令注入
ssh-copy-id $USER@$HOST

# ✅ 先验再拼
if [[ ! "$USERNAME" =~ ^[a-z_][a-z0-9_-]{0,31}$ ]]; then
    log_error "Invalid username: $USERNAME"; exit 1
fi
```

---

## 2. 原子操作三段式：备份 → 修改 → 验证

改变系统状态必须走这个闭环，缺一不可。

### 2a. 备份

```
□ 时间戳命名的备份（永不覆写旧备份）
□ 备份后立即验证完整性（diff / checksum）
□ 备份放在和源文件不同的目录或带唯一后缀
□ 操作前检查备份是否成功，失败就 abort
```

### 2b. 修改

```
□ 用最健壮的匹配方式（正则优先字符串精确匹配，处理空格/tab/注释/大小写）
□ sed / awk / jq 操作后检查返回值
□ 不假设工具一定成功
```

### 2c. 验证

```
□ 修改后验证"值确实变了"
□ 配置文件语法检查（sshd -t / visudo -c / nginx -t / mysqld --validate-config）
□ 服务重启后验证进程存活 + 端口监听
□ 功能级验证（SSH 能不能连、sudo 能不能用、web 能不能访问）
```

### 2d. 回滚

```
□ 上述任何一步失败 → 恢复备份 → 重启服务 → 告警
□ 回滚本身也可能失败 → 有兜底处理（不静默 exit，要打醒目的日志）
□ 给用户明确的"手动修复指南"
```

**反例：** sed 改完直接 `systemctl restart`，不验证语法、不检查进程、不测试连接。

---

## 3. 前置条件检查

"能不能做" 在 "怎么做" 之前。

```
□ 列出操作的前置条件清单
□ 条件不满足 → 拒绝执行 + 告诉用户缺什么 + 怎么补齐
□ 不做"如果没配置就悄悄跳过"的设计（除非加了 --skip-missing 这类显式 flag）
```

**典型前置条件：**

| 操作 | 前置条件 |
|------|----------|
| 关闭 SSH 密码登录 | 至少一个用户配置了 authorized_keys |
| 修改 SSH 端口 | 防火墙规则已更新新端口 |
| 删除用户 | 没有该用户拥有的进程 / 文件 |
| 安装软件包 | 包管理器和仓库可用 |
| 修改 fstab | /etc/fstab 语法检查通过 |
| docker-compose down | 没有正在运行的依赖容器 |

**反例：** `passwdless_ssh.sh` 初版不检查 `authorized_keys` 是否存在，直接关密码登录，服务器失联。

---

## 4. 失败安全（Fail-Safe）

任何东西都可能失败，失败时必须停在安全的一侧。

```
□ 默认拒绝：不确定时拒绝操作，而不是放行
□ 不会因为一个命令失败就悄悄跳过关键步骤
□ 错误信息说清楚三件事：什么失败了、为什么、怎么修
□ 状态不一致时优先恢复到已知安全状态
□ 不要吞掉错误（2>/dev/null 后面总要跟 || log_error）
```

**关键设计规则：**
- 关闭安全功能 → 先确认替代方案可用 → 再关
- 开放端口 → 先配置防火墙规则 → 再启动服务
- 删除数据 → 先备份 → 验证备份 → 再删

---

## 5. 幂等性

同一脚本跑两次不该出问题。

```
□ 操作前检查当前状态，已经是目标状态就 skip
□ 备份不要被二次执行破坏
□ 追加操作检查是否已经存在（grep -qF 去重）
□ 创建目录/文件使用 mkdir -p / install -D 等天然幂等的命令
```

**反例：** `cp source /etc/ssh/sshd_config.bak`（固定文件名），跑两次丢原始备份。

---

## 6. 可观测性

没人能修一个看不见的问题。

```
□ 结构化日志（时间戳 + 级别 + 消息）
□ 三个级别就够：INFO（在干什么）、WARN（要注意但不致命）、ERROR（失败了）
□ 关键决策点都记日志（备份路径、修改了什么值、重启了哪个服务）
□ 日志写文件 + 打终端（tee）
□ 执行结束输出摘要：改了什么、备份在哪、怎么回滚、日志路径
```

**反例：** 脚本跑完只输出一句 `done.`，出问题完全不知道去哪查。

---

## 7. CLI 工程标准

```
□ -h / --help    帮助信息，包含用法、示例、注意事项
□ -n / --dry-run  只预览不执行，打印每一步会做什么
□ -y / --yes      非交互模式，CI/自动化可用
□ -l DIR          日志目录可配置
□ 所有选项有默认值，不需要的参数用默认值而不是报错
```

---

## 8. 安全编码习惯

```
□ set -euo pipefail（Shell）/ try-except + sys.exit（Python）
□ 变量必须引号包裹："$VAR" 而非 $VAR
□ 用 [[ ]] 而非 [ ]（bash）
□ 用 printf 而非 echo 输出变量内容
□ 临时文件用 mktemp，脚本退出时 trap 清理
□ 敏感信息不进命令行参数（会在 ps 里暴露），用 stdin 或环境变量
□ chmod 600 / chmod 700 设置敏感文件和目录权限
□ 不信任命令一定存在（command -v 检查），不信任服务一定叫某个名字
```

---

## 9. 脚本审查清单（新脚本提交前自查）

```
层级 0：骨架
□ 头注释完整（用途、用法、作者、许可证）
□ set -euo pipefail（Shell）或等价严格模式
□ 常量用 readonly / const 声明

层级 1：权限
□ 入口检查运行权限（root/sudo/普通用户）
□ 变量加引号，输入经过验证
□ 没有写死的密码/token/key

层级 2：原子性
□ 改文件前 → 备份（时间戳命名）
□ 改完后 → 验证值生效
□ 语法检查 → 服务重启 → 进程度活 → 端口监听 → 功能测试
□ 任何一步失败 → 自动回滚 + 告警

层级 3：前置条件
□ 危险操作前检查条件是否满足
□ 不满足 → 拒绝 + 引导
□ 有确认提示

层级 4：可观测
□ 结构化日志（INFO/WARN/ERROR + 时间戳）
□ 执行摘要 + 回滚命令 + 日志路径

层级 5：工程
□ -h 帮助 / -n 干跑 / -y 非交互
□ 幂等性验证
□ 跨平台兼容（Debian + RHEL）
```

---

## 10. 本项目的约定

根据已有脚本的风格，统一以下命名：

```
脚本模板头：
#!/bin/bash
#===============================================================================
# Filename: xxx.sh
# Author: JirenYoung
# Date: 2026
# Copyright (c) 2026 JirenYoung. All rights reserved.
# Licensed under the MIT License.
# ...

日志格式：
[2026-06-04 15:30:00] [INFO]  Description of what's happening
[2026-06-04 15:30:01] [ERROR] What failed and why

变量命名：
readonly 常量     UPPER_CASE
普通变量          UPPER_CASE  或  camelCase（选一种保持一致）

日志函数签名：
log_info()  { echo "[$(date '+%Y-%m-%d %H:%M:%S')] [INFO]  $*" | tee -a "$LOG_FILE" >&3 ...; }
log_error() { ... }
log_warn()  { ... }

备份命名：
${ORIGINAL_FILE}.bak.$(date +%Y%m%d_%H%M%S)
```

---

> **一句话记住：脚本是跑在别人的生产服务器上的。你写的每一个 `sed`、`rm`、`systemctl restart` 后面都可能是凌晨三点被叫起来修服务器的运维。对得起他们。**
